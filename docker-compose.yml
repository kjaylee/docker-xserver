version: '3.3'

services:
    nginx:
        restart: always
        build: ./nginx
        container_name: nginx
        ports:
        - 80:80
        - 443:443
        volumes:
        - /app/static
        - /app/media
        - certs:/etc/letsencrypt
        - certs-data:/data/letsencrypt

    
